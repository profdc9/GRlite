{
  "name": "grlite-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GRlite 2D linearized GR sandbox — web frontend (Stage 1)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
